# HyperFormer (modernized dependency set)
#
# Notes:
# - This project was originally pinned to 2020/2021-era PyTorch/Transformers.
# - The codebase has been updated to use modern import paths for Transformers (4.x)
#   and to remove the deprecated `script_version=` argument from 🤗 Datasets.
# - GPU/CPU wheels for PyTorch are platform-specific; install the appropriate
#   torch/torchaudio/torchvision wheels for your CUDA/ROCm setup as needed.

# Core ML / HF stack
torch>=2.1,<3
transformers>=4.37,<5
datasets>=2.16,<3
accelerate>=0.25,<1
sentencepiece>=0.1.99

# Training / metrics
numpy>=1.23,<3
scipy>=1.10,<2
scikit-learn>=1.2,<2
nltk>=3.8
rouge-score>=0.1.2
sacrebleu>=2.3
sacremoses>=0.1.1

# Data / utilities used by datasets + project helpers
pyarrow>=12,<20
protobuf>=3.20,<5
packaging>=23
regex>=2023.10.3
tqdm>=4.66
requests>=2.31
filelock>=3.12
joblib>=1.3
PyYAML>=6.0
GitPython>=3.1.40
portalocker>=2.8

# Optional but commonly used by Transformers' Trainer logging
tensorboard>=2.14
